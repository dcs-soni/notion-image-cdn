{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && bun install --frozen-lockfile && bun run build --filter=web",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping — handled in buildCommand'",
  "framework": "vite"
}
